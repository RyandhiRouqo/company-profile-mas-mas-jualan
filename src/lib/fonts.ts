import { Asap, Roboto_Mono, Truculenta } from 'next/font/google'

export const roboto_mono = Roboto_Mono({
    subsets: ['latin'],
    display: 'swap',
})

export const asap = Asap({
    subsets: ['latin'],
    display: 'swap',
})

export const truculenta = Truculenta({
    subsets: ['latin'],
    display: 'swap',
})